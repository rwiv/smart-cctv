type Account {
    id: Long!
    role: AccountRole!
    username: String!
    #    password: String!
    nickname: String!
    avatarUrl: String!
}

enum AccountRole {
    MEMBER
    ADMIN
}

input AccountCreation {
    role: AccountRole!
    username: String!
    password: String!
    nickname: String!
}
